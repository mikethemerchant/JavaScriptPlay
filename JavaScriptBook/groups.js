class Group {
    constructor (item) {
        const group = [];
        group.push(item);
    }
    add (item) {
        this.group.push(add);
    }

    display () {
        console.log(this.group);
    }
}

let g = new Group('mike');
g.add('ashley');

g.display();